import { AppLayoutNameDirective } from './app-header-name.directive';

describe('AppLayoutNameDirective', () => {
  it('should create an instance', () => {
    const directive = new AppLayoutNameDirective();
    expect(directive).toBeTruthy();
  });
});
