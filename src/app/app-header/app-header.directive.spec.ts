import { AppLayoutDirective } from './app-header.directive';

describe('AppLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new AppLayoutDirective();
    expect(directive).toBeTruthy();
  });
});
