import { AppAboutTextillateDirective } from './app-about-textillate.directive';

describe('AppAboutTextillateDirective', () => {
  it('should create an instance', () => {
    const directive = new AppAboutTextillateDirective();
    expect(directive).toBeTruthy();
  });
});
