import { AppScrollEffectDirective } from './app-scroll-effect.directive';

describe('AppScrollEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new AppScrollEffectDirective();
    expect(directive).toBeTruthy();
  });
});
